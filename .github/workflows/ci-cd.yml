name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest pytest-asyncio httpx scikit-learn joblib

      - name: Train ML Model & Verify Artifact
        run: |
          python train_model.py
          ls -la  # Print files to confirm fraud_model.joblib is generated

      - name: Run Pytest
        run: |
          python -m pytest test_main.py -v --tb=short